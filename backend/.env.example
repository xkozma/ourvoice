PORT=4000
JWT_SECRET=change-this-in-production
# Comma-separated allowed frontend origins
CORS_ORIGIN=http://localhost:5173
# jsonbin.io — create a free account at https://jsonbin.io
# 1. Sign in and go to API Keys to copy your Master Key
# 2. Create a new bin with the contents of backend/data/db.json
# 3. Copy the bin ID from the URL or the bin response
JSONBIN_BIN_ID=
JSONBIN_API_KEY=
